if ! command -v nix_direnv_version >/dev/null 2>&1; then
  echo "sabiql: nix-direnv 3.1 or newer is required for direnv; use 'nix develop' instead" >&2
  return 1
fi

nix_direnv_version 3.1

./scripts/nix-direnv-profile-cleanup.sh .direnv
use flake .
